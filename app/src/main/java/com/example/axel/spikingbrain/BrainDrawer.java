package com.example.axel.spikingbrain;

import android.opengl.GLES30;

import java.io.BufferedReader;
import java.io.File;
import java.io.FileInputStream;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.nio.FloatBuffer;

/**
 * Created by aw980204 on 2017-03-09.
 */

public class BrainDrawer {
    //private final FloatBuffer vertexBuffer;
    //private final int synapseProgram;

    private int mMVPMatrixHandle;

    public BrainDrawer (){
        /*final String vertexShaderCode = getStringFromFile("assets/synapse.shader");
        // prepare shaders and OpenGL program
        int vertexShader = MyGLRenderer.loadShader(
                GLES30.GL_VERTEX_SHADER, vertexShaderCode);
        int fragmentShader = MyGLRenderer.loadShader(
                GLES30.GL_FRAGMENT_SHADER, fragmentShaderCode);

        synapseProgram = GLES30.glCreateProgram();             // create empty OpenGL Program
        GLES30.glAttachShader(synapseProgram, vertexShader);   // add the vertex shader to program
        GLES30.glAttachShader(synapseProgram, fragmentShader); // add the fragment shader to program
        GLES30.glLinkProgram(synapseProgram); */                 // create OpenGL program executables



    }

}
